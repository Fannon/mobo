$extend: /field/_hardwareModelReference

# type and format can be omitted, since form implies type: string and format: Page
form: MultiFunctionUnitModel

sf_form:
  values from category: MultiFunctionUnitModel
